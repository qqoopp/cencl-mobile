import {metrics} from '@/theme/theme';
import styled from 'styled-components/native';

export const NextBtn = styled.TouchableOpacity`
  position: absolute;
  top: ${metrics.statusBarHeight + 15 + 'px'};
  right: 20px;
`;
