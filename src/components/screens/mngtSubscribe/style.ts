import {StyleSheet} from 'react-native';
import styled from 'styled-components/native';

export const styles = StyleSheet.create({
  screen: {
    padding: 20,
  },
});

export const InfoBoard = styled.View`
  border-radius: 14px;
  padding: 15px;
  background-color: #f2f2f2;
`;
